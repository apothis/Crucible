# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# Node / web build artifacts
web/node_modules/
web/dist/

# Generated audio + library data (large, user-specific)
library/
test_output/

# Windows-side run scripts generated by the installers
run_soulx_api.bat
run_diffsinger_api.bat
run_rvc_api.bat

# Local config (has LAN IPs/paths) — copy app_config.example.json -> app_config.json
app_config.json

# Secrets / cookies — never commit
*.binarycookies
cookies.txt
.env
*.env

# OS / editor / tooling
.DS_Store
.claude/

# RemFx de-amp experiment (large checkpoints, external clone)
RemFx/

# SoundFonts for the fluidsynth DI renderer (large, CC0)
soundfonts/

# Secondary venv for RoFormer separation (Python 3.12; torch etc. — large)
.venv-roformer/
# On-SSD caches (torch/HF/pip/uv) — kept off the small system drive
.caches/
# Separation model checkpoints (large)
models/

# local secrets (CLAUDE_CODE_OAUTH_TOKEN etc.)
.env.local
